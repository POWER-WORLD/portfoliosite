services:
  - type: web
    name: portfolio-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm run build
    publishPath: dist
    routes:
      - src: /*
        dest: /index.html
